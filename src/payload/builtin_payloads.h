#pragma once

#include "../pifk_app.h"

/* Number of compiled-in payloads. */
extern const uint16_t BUILTIN_PAYLOAD_COUNT;

/* Array of compiled-in default payloads. */
extern const PifkPayload BUILTIN_PAYLOADS[];
